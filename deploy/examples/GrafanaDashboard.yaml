apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  name: experimental2
  labels:
    monitoring-key: experimental
spec:
  name: dashboard2.json
  json: "{}"
  plugins:
    - name: "grafana-piechart-panel"
      version: "1.3.6"
    - name: "grafana-clock-panel"
      version: "1.0.2"
