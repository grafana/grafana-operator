apiVersion: integreatly.org/v1alpha1
kind: Grafana
metadata:
  name: extra-grafana
  namespace: extra-grafana
spec:
  deployment:
    labels:
      app: extra-grafana
  client:
    preferService: true
  ingress:
    enabled: True
    pathType: Prefix
    path: "/"
    hostName: grafana.extra.unbox.xenit.io
  config:
    log:
      mode: "console"
      level: "error"
    log.frontend:
      enabled: true
    auth:
      disable_login_form: False
      disable_signout_menu: True
    auth.anonymous:
      enabled: True
  service:
    name: extra-grafana
    labels:
      app: extra-grafana
  dashboardNamespaceSelector:
    matchLabels:
      app: extra-grafana
  resources:
    # Optionally specify container resources
    limits:
      cpu: 200m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 100Mi
