apiVersion: v1
kind: ConfigMap
metadata:
  name:  apiserver-config
  namespace: {{.Release.Namespace}}
  labels:
    app: grafana-operator
data:
  config.yaml: |
    region: {{ .Values.global.region}}
    secret_key: "{{ .Values.apiserver.secret_key }}"
    cryptNonce: "{{ .Values.apiserver.crypt_nonce }}"
    grafana: 
        config:
            hostname: "%s.grafana-svc.{{ .Values.global.region}}.cloud.sap"
            logLevel: "{{ .Values.grafana.log_level }}"
            basicAuth: false
            adminPassword: "{{ .Values.grafana.admin_password }}"
            disableLoginForm: true
            autoAssignOrg: true
            orgName: "sapcc"
            grafanaGroupRoleMap: "{{ .Values.grafana.group_role_map }}"
    auth_proxy: 
        connectors: 
            keystone:
                type: keystone
                id: keystone
                name: Converged Cloud
                config:
                  host: "{{ .Values.openstack.auth_url }}"
                  adminUsername: {{ .Values.openstack.admin_username }}
                  adminPassword: {{ .Values.openstack.admin_password }}
                  adminUserDomain: {{ .Values.openstack.admin_user_domainname }}
                  adminProject: {{ .Values.openstack.admin_project }}
                  includeRolesInGroups: true
                  adminDomain: {{ .Values.openstack.admin_domain }}