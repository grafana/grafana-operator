apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaServiceAccount
metadata:
  name: grafana1-sa2
  namespace: ($NAMESPACE)
spec:
  tokens:
    - name: token1
      expires: (time_add($NOW, '20h')) # Add expiration to token1
    - name: token2
      secretName: fff
      expires: (time_add($NOW, '26h')) # Update expiration for token2
    - name: token3
      secretName: eee
    - name: token4
